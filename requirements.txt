# Little Free Library — Python Dependencies
# Install: pip install -r requirements.txt

# Core dependencies
pyyaml>=6.0          # YAML parsing for frontmatter
numpy>=1.21          # Numerical operations for BM25 and scoring
typer>=0.9.0         # CLI framework

# Optional: Embedding support (recommended for vector retrieval)
# Both systems are supported; install one or both based on your use case:
fastembed>=0.2.0   # Lightweight ONNX embeddings (recommended, cross-platform, no PyTorch)
sentence-transformers>=2.2.0  # PyTorch-based embeddings (more models, fine-tuning, GPU optimization)
einops>=0.8.0      # Required by nomic-embed-text model

# Optional: Enhanced tokenization
# Uncomment for accurate token counting:
# tiktoken>=0.5.0   # OpenAI tokenizer (GPT models)

# Development dependencies (optional)
# pytest>=7.0        # Testing
# black>=23.0        # Code formatting
# mypy>=1.0          # Type checking
